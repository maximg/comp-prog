#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;


int main() {
    size_t n; cin >> n;
    int64_t index_product{0};
    
    if (n > 2) {
        vector<int> a(n);
        for (size_t i = 0; i < n; ++i) cin >> a[i];

        // ...
    }
    cout << index_product << endl;
    return 0;
}
